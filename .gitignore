node_modules
.DS_Store
docker-compose.override.yml

# Data files (stored in MongoDB, don't push local data)
data/playerStats.json
data/players.json
data/bannedPlayers.json
data/rooms.json
data/backups/
data/seasons.json
data/adminMessages.json
# Editor-local
.cursor/

# ต้นฉบับ AI art (34MB) — เว็บใช้ .webp ที่ commit แล้ว, .jpg เป็น fallback
# ต้นฉบับอยู่บนเครื่อง + iCloud ไม่ต้องถ่วง repo
public/assets/games/*/*.png
