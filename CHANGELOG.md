# Changelog

## 5.4.0 — 2026-08-06

รอบขัด Coup ให้พร้อมเล่นจริง — ตรวจกติกาซ้ำกับกฎที่เขียนไว้, ทำหน้าวิธีเล่นใหม่, ใส่อนิเมชันประจำจังหวะสำคัญ และเก็บบัคหน้าตาที่โผล่มาจากการชนกันของ CSS กลาง

### กติกา Coup

- **ใครก็ท้าการขวางได้** ไม่ใช่แค่คนที่โดนขวาง — เดิมผิดจากกฎที่ว่า "ใครก็สามารถขอ Challenge ได้"
- **สำรับไม่ปั๊มการ์ดเพิ่มอีกแล้ว** เดิมเมื่อกองกลางหมด `drawCard` สร้างสำรับใหม่ทั้ง 15 ใบ ทำให้เกิดการ์ดซ้ำกับใบที่อยู่ในมือคน — ตอนนี้เติมจากใบที่ยังไม่มีใครถือเท่านั้น
- **แลกเปลี่ยน (ทูต) ไม่นับการ์ดซ้ำสองที่** เดิมการ์ดในมืออยู่ทั้งใน `influence` และ `pendingExchange.options` พร้อมกัน ทำให้จำนวนการ์ดในระบบพองเป็น 17 ใบ
- เพิ่ม property test เล่นสุ่ม 400 ตา ตรวจตลอดทางว่าการ์ดคงที่ 15 ใบ และไม่มีชนิดไหนเกิน 3 ใบ

### วิธีเล่น Coup

- หน้า "วิธีเล่นทุกโหมด" เพิ่มส่วนสอน Coup แบบเห็นภาพ: **ลำดับหนึ่งตาเล่นยังไง 5 ขั้น**, **ท้า กับ ขวาง ต่างกันยังไง** (จุดที่สับสนกันมากที่สุด), **ตัวอย่างเล่นจริง 3 สถานการณ์** และ **มือใหม่พลาดตรงไหนบ่อย** — เดิมเป็นลิสต์สรุปกติกาอย่างเดียว อ่านจบแล้วยังเล่นไม่เป็นเพราะไม่เคยเห็นลำดับการตอบโต้
- modal วิธีเล่นย่อมีลิงก์ไปหน้าเต็ม (เปิดแท็บใหม่ คนที่กำลังเล่นอยู่จะได้ไม่หลุดจากเกม)
- modal วิธีเล่นเขียนใหม่ทั้งใบเป็นสกินของตัวเอง (เดิมยืมสกินของ Black Market มาใช้ สีชนกับธีมม่วง หัวเป็นแดงเลือดหมู)
- โชว์ **รูปการ์ดจริงทั้ง 5 ใบ** แทนกำแพงตัวหนังสือ · แยกกลไก "โกหกได้" ออกมาเป็นกล่องเน้น · กฎ 10 เหรียญเป็นกล่องเตือนแยก
- รวมเป็นไฟล์กลาง `public/js/coupGuide.js` + `public/css/coupGuide.css` หน้ารอห้องกับในเกมใช้ตัวเดียวกัน
- ปุ่ม "📖 วิธีเล่น" ในหน้ารอห้องเคยไม่มี CSS เลย เบราว์เซอร์เลยวาดปุ่มดิบพื้นขาวให้ — ใส่สไตล์ธีมม่วงแล้ว
- เปลี่ยน **"เคาน์เตสส์" เป็น "ท่านหญิง"** ทับศัพท์แล้วอ่านไม่รู้เรื่อง
- หน้า "วิธีเล่นทุกโหมด" เพิ่มหัวข้อ Coup พร้อมรูปการ์ด และแก้บทนำที่ยังเขียนว่ามี 3 โหมด (จริงๆ 5)

### อนิเมชัน Coup

- แจกการ์ดเข้ามือทีละใบ · การ์ดโดนหงายพลิกพร้อมแดงวาบ · ตกรอบสั่นแล้วซีดเป็นขาวดำ
- เหรียญขึ้นลงมีตัวเลขลอย `+3` / `-7` ของทุกคนบนโต๊ะ
- ป้ายประกาศกลางจอตอน **ท้า / ขวาง / รัฐประหาร / ลอบสังหาร / ตกรอบ / ชนะ** — ตั้ง `pointer-events: none` ไม่บังการกด
- ไฟวิ่งรอบกล่องคนที่ถึงตา · นาฬิกาเต้นแดงเมื่อเหลือ ≤5 วิ · บันทึกเกมเลื่อนเข้าเฉพาะบรรทัดใหม่
- เพิ่ม field `kind` ให้เหตุการณ์ในเอนจิน หน้าเว็บจะได้ไม่ต้องเดาชนิดเหตุการณ์จากข้อความ
- ปิดทั้งหมดเมื่อผู้ใช้ตั้งระบบว่าลดการเคลื่อนไหว (`prefers-reduced-motion`)

### บัคหน้าตา — CSS กลางชนกับกระดาน

- **แถบบนของกระดาน Coup และ Spyfall ถูกซ่อนหายทั้งแถว** (เลขห้อง, กองกลาง, วิธีเล่น, ผู้เล่น, จบเกม) เพราะกฎ `html.cp-active header { display: none }` ที่ตั้งใจซ่อนแบนเนอร์ของ layout ดันตรงกับ `<header class="cp-topbar">` ของกระดานเองด้วย
- แก้ที่ต้นเหตุ: เปลี่ยนแถบบนของทั้งสองกระดานเป็น `<div>` — พ้นจากทั้งกฎซ่อนและจาก `style.css` ที่ทาสี `<header>` ตามธีม (ธีมแดงเคยทำให้แถบบนกลายเป็นแถบแดง)
- เปลี่ยน id ปุ่มผู้เล่นเป็น `#cpSidebarBtn` / `#sfSidebarBtn` — เดิมใช้ `#toggleSidebarBtn` ซ้ำกับกระดาน Insider แล้วโดน `mobile.css` บังคับเป็น `position: fixed` จนลอยไปทับปุ่มจบเกมจนกดไม่ได้
- ข้อความไทยยาวๆ ล้นออกนอกกรอบการ์ดในวิธีเล่น (คำไทยไม่มีช่องว่างให้ตัด)
- นาฬิกาใช้ฟอนต์ Orbitron ซึ่งไม่มีสระไทย คำว่า "วิ" เลยขึ้นเป็นกล่องเปล่า — ใส่ฟอนต์สำรอง

### อื่นๆ

- **คำสั่ง `/m` ในแชท Coup** เฉลยการ์ดในมือทุกคน + เหรียญ + ใครถึงตา + การ์ดเหลือในกอง (เหมือน `/m` ของ Insider) ตรวจสิทธิ์ฝั่ง server เฉพาะหัวหน้าห้อง/แอดมินเว็บ ส่งกลับเฉพาะคนที่ขอ และไม่หลุดเป็นข้อความในห้อง
- แถบเวลาบอกล่วงหน้าว่า **ถ้าปล่อยจนหมดเวลาระบบจะทำอะไรให้** (เช่น "หมดเวลา → ถือว่าทุกคนปล่อยผ่าน") โผล่ตอนเหลือ 10 วิ
- หน้าเลือกโหมดเคยโชว์การ์ด Coup ว่า "Insider 2-6 คน" เพราะไม่มีข้อมูลโหมดจึง fallback ไปที่ Insider · หน้ารอห้องก็ไม่รู้จัก Coup เลยใช้เงื่อนไขของ Insider (ต้อง 3 คน)
- **เอาเพลงพื้นหลัง (BGM) ออกทั้งระบบ** ทั้งตัวเล่น ปุ่มควบคุม และการตั้งค่าในหน้า settings
- ตัดศัพท์ที่อ่านแล้วงงออกจากหน้าเลือกโหมด ("อ่านโต๊ะ", "จังหวะ")

### เทสที่เพิ่ม

- `scripts/smoke-coup-guide.js` — ปุ่ม/modal วิธีเล่น, แถบบนไม่โดน CSS กลางเล่นงาน, ปุ่มจบเกมกดโดนจริง (ยิงพิกัดกลางปุ่มเช็คว่าไม่มีอะไรทับ), `/m` ทั้งฝั่งมีสิทธิ์และไม่มีสิทธิ์
- `scripts/smoke-coup-motion.js` — อนิเมชันโผล่แค่เสี้ยววินาที เลยติด MutationObserver ดักไว้ก่อนเกมเดินแล้วค่อยอ่านสิ่งที่จดไว้

## 5.3.0 — 2026-08-05

### Coup — โหมดที่ 5

- เกมโค่นอำนาจ 2–6 คน: การ์ด 5 ชนิด (ดยุค นักฆ่า กัปตัน ทูต ท่านหญิง) ชนิดละ 3 ใบ แก่นเกมคือ "โกหกได้" — ประกาศใช้พลังของการ์ดที่ไม่มีก็ได้ แล้วรอดูว่าจะมีใครท้าไหม
- เอนจินเป็น state machine: `action → respond → block-respond → lose-influence → exchange → finished` พร้อมตัวจับเวลาทุกเฟส หมดเวลาแล้วเดินเกมต่อเองไม่ค้าง
- กระดานธีมม่วงพร้อมแชทกลางตัวเดียวกับโหมดอื่น

### Season / รีแรงค์ leaderboard

- ปิด Season 1 แล้ว — สถิติทุกคนถูกรีเซ็ตเป็น 0 เพื่อเริ่ม Season 2 (อันดับเดิม 387 คนเก็บไว้ครบ ไม่ได้หายไปไหน)
- popup หน้าแรกประกาศรีแรงค์ให้แล้ว — ขึ้นหัวข้อ "🏆 รีแรงค์! เริ่ม Season 2" พร้อมแชมป์ 3 อันดับแรกของ Season 1 และบอกว่าดูอันดับเก่าได้ที่ไหน (key ของ "ไม่แสดงอีก 7 วัน" ผูกกับเลข season แล้ว ทุกคนจึงเห็นประกาศแม้เคยกดปิดไว้)
- leaderboard ในหน้า lobby มีแท็บ season แล้ว สลับดูอันดับสด (Season 2) กับอันดับสุดท้ายของ Season 1 ได้ พร้อมช่วงวันที่และจำนวนคนติดอันดับ
- `managers/seasonManager.js` เก็บประวัติอันดับไว้ที่ `data/seasons.json` แยกจากสถิติสด — ไม่แตะ `playerStats.json` (เป็นไฟล์ local ของแต่ละ environment เหมือน `playerStats.json`/`players.json` ไม่ push ขึ้น repo — ต้องสั่งรีแรงค์บนเครื่อง production แยกต่างหาก)
- API ใหม่ `GET /api/seasons` (รายชื่อ season) และ `GET /api/seasons/:number/leaderboard` (ตารางอันดับย้อนหลัง)
- ปุ่ม **"🏆 ปิด Season / รีแรงค์"** ในแท็บสถิติของหน้า admin — กดปิด season ได้จากเว็บโดยตรง ไม่ต้องเข้า console ของเซิร์ฟเวอร์ มีหน้ายืนยันบอกจำนวนคนที่จะถูก archive/รีเซ็ต พร้อมแชมป์ 3 อันดับแรก และต้องพิมพ์ชื่อ season ให้ตรงก่อนถึงจะกดได้ (ตรวจซ้ำฝั่ง server ด้วย)
- `npm run season:reset` ปิด season ปัจจุบันแล้วเริ่ม season ถัดไป — รันเปล่าเป็น dry run, ใส่ `-- --confirm` ถึงทำจริง และสำรองสถิติดิบไว้ที่ `data/backups/playerStats-season-<n>.json` ก่อนรีเซ็ตเสมอ
- ไฟล์สำรองดึงจาก statsManager โดยตรง จึงได้ครบทั้งตอนใช้ JSON และ MongoDB — และถ้าเขียนไฟล์สำรองไม่สำเร็จจะ**ยกเลิกการรีเซ็ตทันที** (snapshot ใน `seasons.json` เก็บแค่ตารางอันดับ ส่วน `roleStats`/`winByRole`/`modeStats`/`gameHistory` อยู่ในไฟล์สำรองที่เดียว) ข้ามได้ด้วย `--force` ถ้ายอมรับว่าข้อมูลส่วนนั้นจะกู้ไม่ได้

## 5.2.0 — 2026-07-11

ไอคอนภาพใหม่ประจำทุกเกม + รอบแก้บัคใหญ่จากการ audit ระบบทั้ง 3 โหมด (Spyfall, Werewolf, Black Market) เน้นความยุติธรรมของกติกา ผลเกมที่ถูกต้อง และความเสถียรเมื่อมีคนออกกลางเกมหรือ server restart

### ภาพและไอคอน

- ไอคอน SVG ประจำเกมครบทุกโหมด (บทบาท / ไอเทม / สถานที่) เรนเดอร์ผ่านตัวช่วยกลาง `renderGameIcon` ตัวเดียว รองรับทั้ง class string และ options object
- รวมสูตร asset path ไว้ที่ `games/gameAssets.js` ที่เดียว และ role id ที่ไม่รู้จักจะ fallback เป็น emoji แทนรูปแตก

### Spyfall

- สายลับออกจากห้องกลางเกม → จบเกมทันที พลเมืองชนะ (เดิมรอบนั้นจับสายลับไม่ได้เลยเพราะเป้าหายไปแล้ว)
- เลิกสุ่มโหวตแทนคนที่ไม่ได้โหวต — นับเฉพาะโหวตจริง คนบริสุทธิ์ไม่โดนจับมั่วจากโหวตอัตโนมัติ
- การ์ดสายลับบอกเงื่อนไขชนะตรงๆ (เนียนให้รอดโหวต) และรายชื่อสถานที่ระบุชัดว่าเป็นข้อมูลช่วยเดา ไม่ใช่ปุ่ม

### Werewolf

- ศาลเตี้ย/พรานยิงกลางคืน เคารพการป้องกันของหมอ/บอดี้การ์ด/แม่มด/นักบวชแล้ว และยิงตัวเองไม่ได้ (บล็อกฝั่ง server)
- นักสอดแนมไม่รายงานคนที่กด "ข้าม" ว่าใช้สกิลอีกต่อไป และ Oracle ไม่บันทึกผลซ้ำ 2 รายการต่อคืน
- เกณฑ์เสียงโหวตที่ประกาศตรงกับที่ใช้ตัดสินจริงเมื่อนายกเทศมนตรีเปิดตัว (นับ 2 เสียง)
- แก้ป้าย "เข้าสู่คืน N" ที่เกินไป 1 คืน
- เช็คหมาป่าชนะนับตัวตลกเป็นตัวโหวตฝั่งหมู่บ้านด้วย — ไม่ตัดจบเกมก่อนเวลาทั้งที่หมู่บ้านยังโหวตชนะได้ (แต่ยังจบเกมถ้าเหลือแต่ตัวตลก กันเกมค้าง)
- พรนักบวชที่ให้ไปแล้วยังคุ้มครองแม้นักบวชตายก่อนถึงคืนนั้น

### Black Market

- สองคนสุดท้ายสั่งเก็บกันเองตายหมู่ → เกมจบทันทีตัดสินจากอิทธิพล (เดิมเดินยกเปล่าต่อจนครบ 5 ยก)
- สั่งเก็บ/ดีล/หักหลัง/สืบ/ปล้น เป้าที่ตายแล้วถูกปฏิเสธตั้งแต่กด และตอน resolve ก็ไม่เสียปืน/เงินฟรี
- พาสปลอมเป็นไอเทมติดตัวถาวรเหมือนเครื่องดักฟัง (เดิมถูกเผาทิ้งเงียบๆ ตอนหมอบครั้งแรก ทั้งที่ข้อความสื่อว่าเป็นบัฟ)
- ข้อความปุ่มหักหลัง/ปล้น และคำอธิบายนายหน้า/ค่าหัวถึง 5 ตรงกับกลไกจริงแล้ว

### ระบบกลาง

- Restart server แล้วกู้เกมเฉพาะ phase ที่เลยเวลาจริง — ไม่ไล่ resolve หลาย phase รวดจนมีคนโดนโหวตออกทั้งที่เพิ่งกลับเข้าเกม
- ออกเอง/ถูกเตะ/แบน/ถูกลบ กลางเกม → บอร์ดของทุกคนอัปเดตทันที ไม่มี ghost player ค้างเป็นเป้าโหวต/แอคชัน
- เริ่มเกมล้มกลางคัน ไม่ทำให้ห้องติดสถานะ "เกมกำลังเริ่ม" ถาวรอีกต่อไป
- กันกดปุ่มรัวใน Black Market และโหวต Spyfall — ไม่เด้ง error ทั้งที่คำสั่งแรกสำเร็จ
- โหมด dev-fast กัน request เข้าก่อนข้อมูลผู้เล่นโหลดเสร็จ

## 5.1.0 — 2026-06-01

ยกเครื่องระบบการเล่นและ logic engine ใหม่ทุกเกม (Insider, Werewolf, Black Market, Spyfall) เน้นเสถียร เล่นจบไม่ค้าง รีจอยน์/รีโหลดกลางเกมกลับเข้าเฟสเดิมได้ถูกต้อง และ UI บอกชัดว่าตอนนี้ต้องทำอะไร

### Insider engine

- Resync ตอน reconnect/reload ให้ตรงเฟสจริง (ไม่ replay บทบาท/เปิดคำผิดเฟส) — แยกตาม `status` (`in_progress` / `vote2` / `end` / `word`)
- เพิ่ม `advanceInsiderToVote2` ใช้ร่วมกันทั้งปุ่มหยุดเวลาของแอดมินและกรณีหมดเวลาคุย เพื่อกันเกมค้างถ้าแอดมินหลุด
- เก็บ `countdownEndsAt` เพื่อคืนค่าเวลาที่เหลือตอน reconnect และเฉลยคำลับ (`word`) ในจอผลจบเกม
- ข้อความผลเกมเคารพเคส "ไม่มีจอมบงการ" (ghost round) และปรับคำให้ชัด (วง → ทุกคน)

### Werewolf engine

- กลางคืนหมดเวลา: เติมตาให้ครบทุกบท — `oracle` / `tracker` / `vigilante` / `hunter` ที่ AFK นับเป็น "ข้าม" (เดิมตกหล่นเสียตาเงียบ ๆ)
- จบเกมด้วยโหวตกลางวันไม่ขึ้นหัวข้อ "🌙 คืน N+1" อีกต่อไป เปลี่ยนเป็น "⚖️ ผลโหวตปิดเกม"
- แม่มดเปลี่ยน/ยกเลิกเป้าหมายในสกิลเดิมได้ (ปลดสถานะ `locked` ที่ขัดกับคำอธิบายและ server)
- แถบบทบาท (role strip) แสดงครบทุกบทที่ตั้งค่าได้ + เติมบทใหม่อัตโนมัติจาก catalog
- Oracle badge เปลี่ยนสีตามทีมของบทที่เห็น, เพิ่มฮินต์บท tracker/hunter/vigilante
- แชตหมาป่า replay ให้สมาชิกทีมหมาป่าได้ทุกเฟส (แก้ reconnect กลางวันแล้วแชตหาย)
- ปุ่ม night action ปลดล็อกทันทีหลัง ack สำเร็จ ไม่ค้างรอ state broadcast

### Black Market engine

- ลดความรกของบอร์ดสำหรับมือใหม่: ย้ายการ์ดลงมือขึ้นก่อนการ์ดไกด์, เริ่มที่ปุ่มหลักก่อน (ไม่กางแผนขั้นสูงอัตโนมัติ)
- เพิ่มปุ่มนำทาง "⬇️ ไปเลือกของเลย" เลื่อนไปเวทีหลักพร้อมไฮไลต์
- แสดงความคืบหน้าการล็อก (`buildLockProgress` → "ล็อกแล้ว X/Y คน") และอธิบายบทบาท (จุดเด่น + เป้าหมาย) ให้เข้าใจง่าย

### Spyfall engine

- เพิ่มตัวนับความคืบหน้าการโหวต (ลงคะแนนแล้ว X / Y คน)
- ไฮไลต์คนที่เราโหวตชัดเจน และตารางสรุปผลโหวตในจอจบเกม (ระบุสายลับ)

### เทส

- ปรับ `scripts/smoke-werewolf-balance.js` ให้ตรวจ invariant (จำนวนผู้เล่น/จำนวนหมาป่า/บทมาจาก pool) แทนการเทียบชุดบทบาทตายตัว ให้ตรงกับ base role plan ปัจจุบัน

## 4.1.0 — 2026-05-16

### บทบาทและสกิล

- เพิ่มและผูกระบบสกิลให้บทบาทหลายตัวใน **Werewolf engine**: Oracle (อ่านชื่อบทบาทพร้อมประวัติ), Tracker, Hunter, Cleric (พรคุ้มกัน), Vigilante และปรับคำอธิบายบทบาทเดิมให้สอดคล้องเกม
- ปรับ **แผนสุ่มบทบาธรรมดา (ROLE_PLANS)** สำหรับ 3–10 คน ให้มี Villager / Oracle / Tracker / Vigilante ตามจำนวนผู้เล่น
- ช่องตั้งค่าในล็อบบี้รองรับบทบาทใหม่ใน `CONFIGURABLE_ROLE_IDS`

### เกมหลักและเซิร์ฟเวอร์

- **เวลาเฟส**: แก้กรณี `phaseEndsAt` เลยเวลาแต่ไม่ resolve — ให้ `syncWerewolfPhaseTimer` เรียก `autoResolvePhase` ได้ทันที
- **รอบเช้า**: บันทึก `phaseTimerBufferMs` ช่วงประชุมเช้าแล้วผูกกับ `app.js` เพื่อให้รอบมี buffer หลังเล่าเรื่อง (รวมค่า `WEREWOLF_MORNING_RECAP_BUFFER_MS`)
- **ข้ามการโหวตกลางวัน**: ใช้เกณฑ์เสียงข้าม ≥ `floor(น้ำหนักรวม/2)+1` (เสียงข้างมากอย่างถูกต้อง)
- **nightActions / state เก่า**: `ensureActionMaps` เติม key ที่หายหลังอัปเกรดเกมใน memory
- **Socket**: `werewolf_clericBless` เรียก `submitClericBless` ใน engine
- เพิ่ม `console.debug` ช่วยไล่ timer / broadcast (ควรเห็นเฉพาะเมื่อเปิด debug log)

### บอร์ดและล็อบบี้

- หน้า **Werewolf board**: โหมดเลย์เอาต์แบบเดียวกันทั้งจอกว้าง (คอลัมน์กลางจำกัดความกว้าง, FAB แชต, overlay แชต) และปรับ shell fullscreen
- หน้ารายการห้อง / ล็อบบี้: ข้อความหรือ UI เล็กน้อยสอดคล้องบริบท Werewolf

### เทส

- อัปเดต `scripts/smoke-werewolf-flow.js` และ `scripts/smoke-werewolf-role-rules.js` ให้ครอบกติกาและเฟสใหม่
- Smoke flow: loop โหวตไล่หมาป่ารองรับห้องมีมากกว่า 1 หมาป่าแบบในแผนปัจจุบัน
- Smoke roles: key ผลทดสอบ Tracker vs Cleric (`trackerClericNoNightSkillMisread` / `trackerSeerShowsNightSkillUsed`)
